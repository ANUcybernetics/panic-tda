defmodule PanicTda.Repo.Migrations.AddNetworksToExperiments do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_sqlite.generate_migrations`
  """

  use Ecto.Migration

  def up do
    drop_if_exists unique_index(:runs, [:experiment_id, :initial_prompt, :run_number],
                     name: "runs_unique_experiment_run_index"
                   )

    create unique_index(:runs, [:experiment_id, :network, :initial_prompt, :run_number],
             name: "runs_unique_experiment_run_index"
           )

    alter table(:experiments) do
      remove :network

      add :networks, {:array, {:array, :text}}, null: false
    end
  end

  def down do
    alter table(:experiments) do
      remove :networks

      add :network, {:array, :text}, null: false
    end

    drop_if_exists unique_index(:runs, [:experiment_id, :network, :initial_prompt, :run_number],
                     name: "runs_unique_experiment_run_index"
                   )

    create unique_index(:runs, [:experiment_id, :initial_prompt, :run_number],
             name: "runs_unique_experiment_run_index"
           )
  end
end